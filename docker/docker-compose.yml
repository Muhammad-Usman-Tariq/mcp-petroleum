version: "3.9"

services:
  mcp-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
      - "8001:8001"
    env_file:
      - ../.env
    restart: unless-stopped